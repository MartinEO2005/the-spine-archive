// Importamos directamente desde la carpeta public (o donde tengas el JSON)
import juegosData from '../../public/nombres_juegos.json';

// Exportamos la constante que usa tu componente AboutView
export const FUN_FACTS = juegosData;